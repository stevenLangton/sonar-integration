@model List<String>

@{
    ViewBag.Title = "ShowClaims/ShowKeys";
}

<h2>ShowClaims/ShowKeys</h2>
<table>
    @foreach (var claimString in Model)
    {
        <tr>
            <td>
                @claimString
            </td>
        </tr>
    }
</table>
